// @flow

/**
 * These types are used by the helper functions to control types using flow
 */
type Argument = ?string | ?number | ?boolean;
type Arguments = Array<Argument>;

export type { Argument, Arguments };
